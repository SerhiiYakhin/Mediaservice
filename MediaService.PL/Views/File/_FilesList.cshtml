@using MediaService.BLL.DTO.Enums
﻿@model IEnumerable<MediaService.BLL.DTO.FileEntryDto>

<table class="table">
    <tr>
        <th>
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.ParentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Size)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Created)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Downloaded)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Modified)
        </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                <img width="50" height="50"
                     src="@(item.FileType == FileType.Image ? Url.Content("/fonts/icons-buttons/picture.svg") : Url.Content("/fonts/icons-buttons/video-player.svg"))">
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <th>
                <img src='@item.FileThumbnailLink' />
            </th>
            @*<td>
        @Html.DisplayFor(modelItem => item.ParentId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Size)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Created)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Downloaded)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Modified)Guid fileId, Guid parentId, string fileName
    </td>*@
            <td>
                @Html.ActionLink("Add Tag", "AddTag", "File", new { fileId = item.Id}, null ) |
                @Html.ActionLink("Rename", "Rename", "File", new { fileId = item.Id, parentId = item.ParentId, fileName = item.Name }, null) |
                @Html.ActionLink("Delete", "Delete", "File", new { fileId = item.Id }, null) |
                @Html.ActionLink("Download", "Download", "File", new { fileId = item.Id, fileSize = item.Size }, null)
            </td>
        </tr>
    }

</table>